region: us-west-2

profile: default
accountId: 

teamRole: VMWMasterRole
teamPolicy: arn:aws:iam::aws:policy/AdministratorAccess

company: vmware
team: vrg


#deploymentBucket: #<optional S3 bucket> # comment out the use of this variable in your serverless.yml files to deploy instead to a default bucket generated by the Serverless Framework

## Core Stream
#coreStream:
#  accountId: #<Core Retail Stream's Account ID>
#  awslabsRoleArn: arn:aws:iam::${self:custom.private.coreStream.accountId}:role/fanoutRole
